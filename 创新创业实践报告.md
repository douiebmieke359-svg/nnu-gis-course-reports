# 南 京 师 范 大 学

# 研 究 生 课 程 学 习 考 试 成 绩 单

## （试卷封面）

# 面向地学科研的地理信息智能体（GeoAsk Agent）开发实践

## 一、实践背景与实践目的

## 1.1 实践背景

地图学与地理信息系统专业的研究生，日常科研里真正卡住的往往不是“会不会某个空间分析工具”，而是“把一个用自然语言表达的地学问题，翻译成正确的数据、坐标系、分析工具和一张可复现的图”。例如：“把这些采样点投影到CGCS2000，做500米缓冲区，再统计落在缓冲区内的POI数量，并出一张热点图。”在ArcGIS或QGIS里，这需要一串菜单操作；写成Python脚本，又要反复查GeoPandas接口。通用大语言模型看起来能对话，但直接把坐标表贴进网页对话有三层风险：一是未发表的调查点、内部课题数据存在泄露；二是模型会编造坐标、面积和“显著热点”，却给不出可检验的中间结果；三是模型不懂投影——在经纬度下用“500米”做缓冲，空间结论从第一步就错了。

与此同时，地理信息科学正在进入Agent（智能体）阶段。学院旭旦讲堂、陈述彭大讲堂近两年的报告，反复提到GeoAI必须是地理显式的：位置、尺度、空间关系和过程不能只作为文本出现在提示词里，而要进入可调用的分析工具。大模型擅长理解任务和调度步骤，GIS库擅长做几何、投影和空间统计。二者拆开都有明显短板，合在一起才构成一个能在科研场景里落地的地理信息Agent。基于这一判断，本次创新创业实践不开发又一个菜单式GIS，而是做一个轻量原型智能体“地问 GeoAsk”：用自然语言下达地学分析任务，由Agent规划步骤、调用本地GIS工具、返回带参数日志的结果与地图，原始空间数据默认不出本机。

## 1.2 实践目的

技术创新层面：把大语言模型的任务理解能力，与本地地理处理工具链耦合，形成“自然语言 → 工具规划 → 投影校验 → 空间计算 → 制图与可复现日志”的Agent闭环；针对幻觉、坐标系错误和空间单元不清这三个地学场景特有问题，在工具层而不是提示词层做硬约束。

工程实践层面：完整走完小型智能体从需求分析、工具注册、规划循环、界面开发、测试调试到原型打包的流程，掌握Function Calling、GIS脚本化和桌面交互开发。

专业结合层面：迫使自己把缓冲区、叠加、核密度、热点分析从“软件里的一个按钮”变成Agent可调用、带前置条件的工具，从而真正理解每种分析对坐标系、尺度和样本量的要求。

创新创业能力层面：从研究生真实痛点出发，验证“地理信息Agent”这一微型产品是否成立。不追求对标商业Copilot，只证明：在本地数据不出域的前提下，自然语言驱动的空间分析原型可以被做出来，并且有明确的适用边界。

## 二、实践主要内容与实施过程

实践分为九个阶段：需求分析、技术选型、GIS工具注册、Agent规划循环、地理约束与安全策略、对话与地图界面、样例任务测试、缺陷修复、打包。内部名称定为“地问 GeoAsk”，定位为面向地学研究生的本地地理信息智能体原型，而不是通用聊天机器人。

## 2.1 需求分析

将用户故事收敛为一句话：研究生对着一份本地点/线/面数据，用中文说出想做的分析，Agent在本机完成计算并给出可检查的结果。据此列出必须具备的能力。（1）对话式任务理解：支持中文指令，如“把csv里的经纬度转成CGCS2000高斯克吕格，做800米缓冲，统计每个缓冲里有多少POI”。（2）本地数据接入：CSV、GeoJSON、Shapefile，数据文件路径只在本机读写。（3）可调用GIS工具集：坐标系识别与变换、缓冲区、空间连接/叠加计数、核密度、Getis-Ord Gi* 热点、简单制图导出。（4）规划可见：执行前展示工具调用计划（用哪个工具、关键参数），允许用户确认或改参数。（5）结果可复现：每次运行写入EPSG、带宽、缓冲半径、点数、输出路径。（6）防幻觉：涉及面积、距离、数量、显著性的数字，必须来自工具返回值，禁止模型口算或编造。（7）隐私：原始坐标与几何默认不发送给云端模型，仅发送字段名、坐标系、统计摘要和工具结果。不做的事情同样写清：不支持复杂网络分析、三维场景、全自动论文写作，也不假装能替代专业GIS。

## 2.2 技术方案选型

总体架构采用“规划—执行—观察”（Plan-Act-Observe）循环，而不是把全部GIS逻辑写进提示词。大模型只负责理解用户意图、选择工具、填写参数和用中文解释工具返回的结果；真正的空间计算全部落在Python工具函数上。

语言模型：原型提供双通道。默认优先走国产大模型的Function Calling接口（通义千问或DeepSeek），延迟低、中文指令跟随较好；无网或数据极度敏感时，可切换到本机部署的中小参数对话模型。无论哪条通道，发送内容都经过脱敏：不传原始坐标串，只传工具名、参数和汇总统计。地理处理层使用GeoPandas、Shapely、PyProj、SciPy；制图使用Matplotlib。界面采用PyQt5，左侧为对话与计划确认，右侧为地图预览和日志。Agent框架不引入过重依赖，自行实现一个最小工具注册表与多轮循环，便于在报告中把控制流写清楚，也避免被框架版本绑定。

## 2.3 模块开发实现

（1）GIS工具层：把地学方法做成带前置条件的函数

这是本原型相对“聊天式GIS问答”的核心差异。每个工具在注册时声明：名称、中文说明、参数模式、是否要求平面坐标系、最小样本数。例如buffer、kernel_density必须在投影坐标系下运行，单位为米；load_data负责读入并记录CRS；project_to负责变换；spatial_count做缓冲区内计数；hotspot做Gi*；make_map出图。工具内部做硬校验：缺少CRS则拒绝执行并返回错误信息给Agent，由Agent转成人话提示用户，而不是“猜一个默认值继续算”。这样，地理约束进入的是执行层，而不是仅仅写在系统提示里——提示词约束会被模型忽略，函数里的raise不会。

（2）Agent规划循环

用户输入后，系统将工具清单、当前数据目录（图层名、字段、EPSG、点数，不含坐标）和对话历史发给模型。模型以结构化方式返回下一步：或调用某个工具，或向用户追问缺失参数，或结束并生成说明。程序解析调用、在本地执行、把工具的JSON摘要（成功/失败、n、mean、max、输出文件）观察回模型，进入下一轮，最多限制步数防止循环。涉及破坏性或高代价操作（重投影写盘、出图覆盖）前，界面弹出计划卡片，用户确认后才真正执行。这一步把“智能体”和“自动乱跑脚本”区分开，也符合科研里对参数负责的要求。

（3）地理约束与防幻觉策略

系统提示明确规定三条铁律。第一，凡距离、面积、数量、p值，只能引用最近一次工具返回，禁止估算。第二，用户说“附近”“较大范围”等模糊词时，不得擅自代入数值，必须追问或给出默认方案请用户确认（如“附近”默认500米，请确认）。第三，不得把行政区、热力图色斑直接解释为人群暴露或因果关系——这一点直接对应近期学术报告中对UGCoP和“相关当作机制”的批评，Agent在总结里需提示结果的尺度与局限性。工具结果进入提示词前会做裁剪，避免把整表坐标回灌给模型。

（4）对话—地图界面

主窗口分三块：对话区支持多轮中文；计划区以列表展示即将调用的工具和参数，提供确认/改参/取消；地图区渲染当前图层与最新分析结果，底部日志同步EPSG、半径、带宽和导出路径。典型交互不是“一句话出最终结论”，而是：用户提问 → Agent给出计划 → 用户改500米为800米 → 执行 → 地图更新 → Agent用中文解释数字来源。保留人在回路，是地学Agent在原型阶段必须做的取舍：空间分析错一次，论文里就会错一张图。

## 2.4 测试、调试与打包

测试任务使用三类脱敏样本：城市POI、课程实习采样点、模拟事件点。设计了固定话术集，例如“导入点并检查坐标系”“在未投影数据上直接缓冲”“做500米缓冲并计数”“换带宽看核密度变化”“问一个模型不该口算的面积”。

测试暴露的问题很典型。其一，模型经常在地理坐标系下直接填buffer_radius=500，早期版本如果只把校验写在提示词里，它仍会调用；直到工具函数拒绝执行并返回明确错误，Agent才会去调project_to。其二，中文指代不清，“把这些点”在多图层时会选错表，后来在上下文里强制带上当前图层名，并在计划卡里用中文写图层名供确认。其三，模型喜欢把核密度色斑说成“高风险区”，后来在总结模板里加入禁止因果化的提示，并在日志中写“探索性可视化，不作为风险评价结论”。其四，云端接口偶发超时，本地小模型跟指令不稳定，最终做成双通道和“失败则展示已执行的工具结果，不编造后半段”。其五，打包后依赖体积大，Function Calling所需网络在无网实验室不可用，文档中写明“在线规划+本地计算”和“纯本地简化规划”两种运行模式。

原型边界需要写进报告：地问只能调度已注册的有限工具，不能发明新算法；复杂拓扑、网络分析和地理加权回归仍应回到专业软件或专门脚本；它适合的是几十到数万要素的课题数据上的探索性分析与方法学习，不适合作为生产环境的自动制图流水线。

## 三、实践创新亮点与核心实践成果

## 3.1 创新亮点

范式创新：从“人去点GIS菜单”转为“人提地学问题、Agent调度GIS工具”。创新点不在于又训练了一个大模型，而在于把地理信息专业方法封装成可被语言模型调用、同时能拒绝错误调用的工具。这对应的是“地学 × AI”而不是“AI + 地学贴图”。

地理约束进执行层：投影、单位、最小样本、模糊距离必须确认，这些规则写在工具函数和计划确认里，而不是指望模型自觉遵守。针对大模型最容易在空间问题上翻车的地方设防，这是面向地学场景的Agent和通用聊天助手的本质差别。

安全与可复现：原始几何不出域，数字来自工具，参数写入日志。对研究生手头未发表数据，这比多一个花哨对话技能更关键。可复现日志也让课程作业的方法部分写得清：用了什么EPSG、半径、带宽，而不是只写“借助AI完成分析”。

轻量产品思维：只做点/面数据上最高频的分析闭环，保留人在回路。不把原型包装成“一句话生成论文”或“智慧城市操作系统”。先证明痛点成立、路径跑通，再谈要不要产品化。

## 3.2 阶段性实践成果

代码成果：完成GeoAsk原型，包含工具注册表、规划循环、投影硬校验、对话—计划—地图界面和运行日志模块，关键代码有注释，可在普通笔记本上演示。

软件原型成果：可启动的桌面程序，支持本地数据导入、中文下达分析任务、确认工具计划、查看地图与导出图件。

测试任务集：整理十余条典型中文指令及对应的正确工具序列，作为回归测试，也作为给同学演示时的脚本，避免现场只靠“聊天感觉”评价Agent。

实践产出：已用该原型完成课程作业中的点数据探索（投影、缓冲计数、核密度出图），方法部分能够写明由Agent调度、由本地工具计算、参数经过人工确认。它把我从反复查接口，转向检查空间分析前提是否成立。

## 四、实践过程遇到的问题以及解决方法

问题1：大模型在经纬度数据上直接做以米为单位的缓冲区，结果形态错误。解决方法：工具层硬校验CRS，未投影则返回错误并建议调用project_to；计划卡上用红字提示当前单位是“度”还是“米”。

问题2：模型编造面积、POI数量或“99%显著热点”。解决方法：系统提示禁止口算；界面展示的数字只绑定工具JSON；若本轮没有调用计数/热点工具，总结模板不得出现这类数字。

问题3：自然语言里的“附近”“较大”“重点区域”没有定量含义，Agent擅自代入参数。解决方法：模糊词触发追问；若用户跳过，采用明示默认值（附近=500米）并写进日志，不允许静默猜测。

问题4：多图层时指代消解失败，缓冲了错误的点层。解决方法：上下文始终带当前图层清单；计划确认阶段必须出现图层中文名；执行前允许下拉框改选。

问题5：云端模型与本地小模型表现不稳定，打包体积大。解决方法：双通道、步数上限、失败时保留已成功的工具结果；文档区分演示环境与无网环境，不把“永远连得上大模型”写成已经解决的指标。

## 五、实践总结与个人收获

## 5.1 技术层面

本次实践让我把大语言模型从“会聊天的搜索框”，落到一个可调试的地理信息Agent上。真正有技术含量的部分，不是调一条生成式API，而是：如何把缓冲区、投影、热点分析定义成带前置条件的工具，如何让模型的每一步都可以被看见、被改、被记录。调试过程也加深了对地学分析出错机制的理解——很多时候不是算法不够新，而是坐标系、尺度和空间单元没有被当作一等公民。

## 5.2 创新创业思维层面

地理信息领域的Agent容易被讲成宏大叙事。这次实践让我意识到，创业意义上的切入点可以很小：帮研究生把一句中文分析需求，安全地跑完本地GIS工具。验证的是需求是否高频、路径是否可信、用户是否愿意在执行前多看一眼计划。原型负责证明“这件事值得做”，产品才需要考虑模型成本、许可和多用户协作。分清这两步，本身就是创新创业课要训练的判断力。

## 5.3 专业科研结合层面

GeoAsk没有停留在演示对话。我已用它处理课程作业中的点格局问题，并在写方法时保留工具参数。它也改变了我阅读GeoAI文献的方式：开始区分“用AI预测一张图”和“用Agent调度有地理约束的分析”。后者更接近学院近年强调的机理与数据双驱动——语言模型提供调度，地学工具提供机制与度量。

## 5.4 综合能力层面

锻炼了需求裁剪、接口设计和测试思维。必须做的是工具硬校验、计划确认、数字来自工具、数据不出域；明确不做的是自动写论文、全要素GIS、无监督的因果结论。会取舍，比把演示做成无所不能更重要。

## 六、实践小结

本次创新创业实践立足于地图学与地理信息系统研究生的真实科研痛点，开发了轻量地理信息智能体原型“地问 GeoAsk”。它用自然语言理解地学分析任务，在本地调用投影、缓冲、叠加计数、核密度和热点等工具，并以计划确认、参数日志和地图预览保证过程可检查、数据不出域。实践完整覆盖了需求分析、技术选型、工具化封装、Agent循环、测试与打包。原型距离成熟产品仍有明显差距：工具覆盖面有限、模型不稳定、打包体积偏大。但它已经能够服务本人课程作业中的探索性空间分析，也证明了地理信息Agent的关键不在“更会聊天”，而在“把地学规则做成模型无法绕开的工具”。后续迭代将优先扩充工具注册、加强轨迹/活动空间类分析，并继续守住原始几何不出域这条底线。

## 附录  核心关键代码片段

1. GIS工具注册：投影硬校验

import geopandas as gpd
from pyproj import CRS

TOOLS = {}

def tool(name, need_projected=False):
    def deco(fn):
        TOOLS[name] = {"fn": fn, "need_projected": need_projected}
        return fn
    return deco

def ensure_projected(gdf):
    if gdf.crs is None:
        raise ValueError("缺少坐标系，请先调用 project_to。")
    if CRS.from_user_input(gdf.crs).is_geographic:
        raise ValueError("当前为地理坐标系，度量分析前必须投影。")
    return gdf

@tool("project_to")
def project_to(gdf, epsg=4547):
    out = gdf.to_crs(epsg=epsg)
    return {"ok": True, "epsg": epsg, "n": len(out)}, out

@tool("buffer", need_projected=True)
def buffer_layer(gdf, radius_m):
    gdf = ensure_projected(gdf)
    out = gdf.copy()
    out["geometry"] = out.buffer(float(radius_m))
    return {"ok": True, "radius_m": radius_m, "n": len(out)}, out

2. Agent规划循环（数字只能来自工具）

SYSTEM = """你是地理信息智能体地问GeoAsk。
规则：
1. 距离/面积/数量/p值必须来自工具返回，禁止口算或编造。
2. 度量类工具前必须确认图层已投影到平面坐标系。
3. “附近”“较大”等模糊词先追问，或给出默认值请用户确认。
4. 不得把核密度或热点直接说成风险或因果关系。
"""

def run_agent(user_text, layers, llm):
    obs = {"layers": summarize_layers(layers)}  # 不含原始坐标
    history = [{"role": "user", "content": user_text}]
    for step in range(8):
        plan = llm.plan(SYSTEM, history, list(TOOLS), obs)
        if plan["type"] == "final":
            return plan["text"], layers
        if plan["type"] == "ask_user":
            return plan["text"], layers
        name, args = plan["tool"], plan["args"]
        if not confirm_plan(name, args):
            return "已取消执行。", layers
        summary, layers = dispatch(name, args, layers)
        history.append({"role": "tool", "content": str(summary)})
        obs["last_tool"] = summary
    return "达到步数上限，请检查计划。", layers

3. 对话界面：先确认计划再执行

from PyQt5.QtWidgets import QMainWindow, QMessageBox

class GeoAskWindow(QMainWindow):
    def on_send(self):
        text = self.input.toPlainText().strip()
        if not text:
            return
        plan = self.agent.preview(text, self.layers)
        msg = "即将执行：\n" + "\n".join(
            f"- {p['tool']} {p['args']}" for p in plan["steps"]
        )
        if QMessageBox.question(self, "确认分析计划", msg) != QMessageBox.Yes:
            self.log.append("用户取消")
            return
        answer, self.layers = self.agent.run(text, self.layers)
        self.chat.append("地问：" + answer)
        self.redraw_map()
        self.log.append(self.agent.last_log)
